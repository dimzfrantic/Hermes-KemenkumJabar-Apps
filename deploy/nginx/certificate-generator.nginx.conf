server {
    listen 80;
    server_name certgen.example.local;
    location / {
        proxy_pass http://127.0.0.1:5062;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
